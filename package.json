{
  "name": "figma-token-studio",
  "version": "1.0.0",
  "scripts": {
    "global": "npx token-transformer tokens.json transform/global.json global",
    "button": "npx token-transformer tokens.json transform/button.json button",
    "semantic":  "npx token-transformer tokens.json transform/semantic.json semantic",
    "semantic-dark": "npx token-transformer tokens.json transform/semantic-dark.json semantic-dark",
    "run": "npm run global && npm run button && npm run semantic && npm run semantic-dark"
  }
}
